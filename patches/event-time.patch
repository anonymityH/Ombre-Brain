diff --git a/src/import_memory.py b/src/import_memory.py
--- a/src/import_memory.py
+++ b/src/import_memory.py
@@
     async def _create_import_bucket(self, item: dict) -> str:
         """创建一条导入记忆；importance 只是普通评分字段，不设硬配额。"""
@@
             name=item.get("name") or None,
             source_tool="import",
             event_actor="human",
             imported=True,
+            event_time=item.get("event_time", ""),
+            event_time_end=item.get("event_time_end", ""),
         )
@@
         if not items:
             return True
+
+        # Keep source chronology separate from the time OB creates the bucket.
+        # For ordinary conversation imports the export timestamp is trusted
+        # provenance.  The LLM may provide a more specific event_time, but it
+        # must never invent one when the source has no timestamp.
+        source_event_time = str(chunk.get("timestamp_start") or "").strip()
+        source_event_time_end = str(chunk.get("timestamp_end") or "").strip()
+        for item in items:
+            if source_event_time and not item.get("event_time"):
+                item["event_time"] = source_event_time
+            if source_event_time_end and not item.get("event_time_end"):
+                item["event_time_end"] = source_event_time_end
 
         # --- 逐条保存提取出的记忆 ---
@@
                 "is_pattern": parse_bool(
                     item.get("is_pattern", False), default=False
                 ),
+                "event_time": str(item.get("event_time") or "").strip(),
+                "event_time_end": str(item.get("event_time_end") or "").strip(),
             })
 
diff --git a/src/bucket_manager.py b/src/bucket_manager.py
--- a/src/bucket_manager.py
+++ b/src/bucket_manager.py
@@
 _METADATA_TEXT_LIMITS = {
@@
     "_pre_anchor_source_tool": _SOURCE_TOOL_MAX,
+    "event_time": 64,
+    "event_time_end": 64,
 }
@@
         imported: bool = False,
+        event_time: str = "",
+        event_time_end: str = "",
         source_refs: Any = None,
@@
         if imported:
             metadata["imported"] = True
+        for field, value in (
+            ("event_time", event_time),
+            ("event_time_end", event_time_end),
+        ):
+            text = self._sanitize_text(str(value or "")).strip()[
+                :_METADATA_TEXT_LIMITS[field]
+            ]
+            if text:
+                metadata[field] = text
@@
                   # release 时自动恢复；None 表示删除该字段。
-                  "source_tool", "grow_batch_id", "last_merged_by", "_pre_anchor_source_tool",
+                  "source_tool", "grow_batch_id", "last_merged_by", "_pre_anchor_source_tool",
+                  # 历史事件时间与 OB 自身 created/last_active 分离。
+                  # 仅透传来源已知的时间；导入层不得用导入时刻冒充。
+                  "event_time", "event_time_end",
                   # I 沉淀机制字段（tools/i/core.py 维护，bucket_manager 不生成也不解读）：
